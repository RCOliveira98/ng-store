import { RcoColorPrimaryDirective } from './rco-color-primary.directive';

describe('RcoColorPrimaryDirective', () => {
  it('should create an instance', () => {
    const directive = new RcoColorPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
